package net.guizhanss.fastmachines.utils

// Item ids in this addon's lang files are already the same all-caps snake_case as the Slimefun item
// id (see lang/en-US.yml), so no transformation is needed here.
fun String.toId() = this
